spring:
  application:
    name: loginTest

  datasource:
    username: test_user
    password: test_password
    url: jdbc:mysql://localhost:3323/test_db

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: create
